#ifndef Generator_h
#define Generator_h

#include <list>
#include <vector>
#include <algorithm>

static const unsigned int _N = 9;

bool generateWithSpecifiedPattern(int board[_N][_N], bool pattern[_N][_N]);

#endif